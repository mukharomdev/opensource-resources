#!/usr/bin/bash

clear

echo "this is bash script from system"
echo
echo "this computer owned by $USER"

echo "Today's date is `date`, this is week `date +"%a"`."
echo

echo "This is `uname -s` running on a `uname -m` processor."
# echo "$PATH"
echo $LINE = $#
